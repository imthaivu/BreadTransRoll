rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // WARNING: Public access. For development only.
      allow read, write: if true;
    }
  }
}


